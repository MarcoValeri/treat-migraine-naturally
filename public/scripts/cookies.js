window.onload = () => {

    // Define variables that have DOM elements as values
    const banner = document.getElementById('banner');
    console.log(`This is banner: ${banner}`);

    /*
    * Create an event that shows the cookie policy banner in the footer 
    * if the user does not accepted it before
    */
   show_cookie_policy.addEventListener('click', e => {

        

   });


}